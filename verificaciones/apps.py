from django.apps import AppConfig


class VerificacionesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'verificaciones'
    verbose_name = 'Verificaciones de Datos'